import Controller from '@ember/controller';
import  {get, set } from '@ember/object';

export default Controller.extend({
    // allWorkers: "foo",
    // async init() {
    //     this._super(...arguments);
    //     await set(this, 'allWorkers', this.store.findAll('hibernate_worker'));
    // }
});
